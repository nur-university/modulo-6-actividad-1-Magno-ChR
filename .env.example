# API Gateway Environment Variables

# Entorno de ejecución
ASPNETCORE_ENVIRONMENT=Development

# URLs del servicio
ASPNETCORE_URLS=http://+:8080;https://+:8081

# Configuración JWT (si se desean cambiar en runtime)
JWT_SECRET_KEY=your-super-secret-key-that-is-at-least-32-characters-long-for-hs256
JWT_ISSUER=api-gateway
JWT_AUDIENCE=api-gateway-users
JWT_EXPIRATION_MINUTES=60
